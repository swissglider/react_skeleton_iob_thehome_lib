name: Test

on:
  push:
    # tags:
    #   - 'Test*'
    #   - 'Release*'

jobs:
  # justEcho1:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: echo "schaumer mal"
  # justEchoTest:
  #   if: startsWith(github.ref, 'refs/tags/Test')
  #   needs: [justEcho1]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Just echo Test
  #       run: echo "Hallo Test ${{ github.ref }}"
  # justEchoRelease:
  #   if: startsWith(github.ref, 'refs/tags/Release') 
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Just echo Release
  #       run: echo "Hallo Test ${{ github.ref }}"
  file_contents:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v1
      - name: Just echo Test
        run: pwd && ls -la
      - name: Read file contents
        id: read_file
        uses: juliangruber/read-file-action@v1
        with:
          path: ./test.test
      - name: File contents
        run: echo "${{ steps.read_file.outputs.content }}"
      - name: MYVARIABLE
        run: sed -n -e '/$string1/ s/.*\= *//p' ./test.test
      - name: MYVARIABLE1
        run: sed -n -e '/$string2/ s/.*\= *//p' ./test.test
      - name: Output
        run: echo "${{steps.MYVARIABLE}} - ${{steps.MYVARIABLE1}}"
